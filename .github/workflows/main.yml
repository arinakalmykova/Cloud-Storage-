name: CI/CD - AuthService

on:
  push:
    branches: [ main, master ]
    paths:
      - 'backend/AuthService/**'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'backend/AuthService/**'

jobs:
  php-analysis:
    runs-on: ubuntu-latest
    name: PHP Static Analysis
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, sqlite, tokenizer, xml
          tools: composer, phpcs, phpstan
      
      - name: Install dependencies (ignore missing extensions)
        working-directory: backend/AuthService
        run: composer install --no-interaction --no-progress --ignore-platform-req=ext-rdkafka
      
      - name: PHP_CodeSniffer (PSR-12)
        working-directory: backend/AuthService
        run: ./vendor/bin/phpcs app/ --standard=PSR12 || true
      
      - name: PHPStan
        working-directory: backend/AuthService
        run: ./vendor/bin/phpstan analyse app/ --level=8 --no-progress || true
      
      - name: Install Lizard
        run: pip install lizard
      
      - name: Cyclomatic Complexity
        working-directory: backend/AuthService
        run: lizard app/ -C 10 || true

  php-tests:
    runs-on: ubuntu-latest
    name: PHP Unit Tests
    needs: php-analysis
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, sqlite, tokenizer, xml
          coverage: xdebug
      
      - name: Install dependencies (ignore missing extensions)
        working-directory: backend/AuthService
        run: composer install --no-interaction --no-progress --ignore-platform-req=ext-rdkafka
      
      - name: Create database
        working-directory: backend/AuthService
        run: |
          mkdir -p database
          touch database/database.sqlite || true
      
      - name: Run tests
        working-directory: backend/AuthService
        run: ./vendor/bin/phpunit --testdox || echo "No tests found"