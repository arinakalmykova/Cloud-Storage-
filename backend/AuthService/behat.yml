default:
    autoload:
        '': '%paths.base%/features/bootstrap'
    suites:
        auth:
            paths:
                - features/auth
            contexts:
                - FeatureContext