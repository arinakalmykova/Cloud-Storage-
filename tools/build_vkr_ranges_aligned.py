from __future__ import annotations

from pathlib import Path

from docx import Document

from update_vkr_sources import (
    append_citations,
    normalize_document_font,
    replace_math_text,
    replace_source_list,
)


CITATIONS = {
    48: [4],
    119: [6, 7],
    125: [13, 14],
    248: [4],
    253: [8, 12],
    254: [1, 11],
    279: [1, 2, 3],
    394: [10, 11],
    398: [5],
    477: [7, 13, 14, 16, 17],
    478: [13, 14, 16, 17],
    479: [6, 8, 13, 14, 16, 17],
    486: [8, 12],
    496: [8, 12],
    821: [9, 15],
}


def apply_citations(document: Document) -> None:
    for idx, numbers in CITATIONS.items():
        append_citations(document.paragraphs[idx], numbers)


def update_chapter_two(document: Document) -> None:
    document.paragraphs[30].text = (
        "Тема выпускной квалификационной работы «Облачный сервис для архивного хранения фотографий». "
        "Отчет состоит из 88 страниц, 17 источников, 20 таблиц, 50 рисунков."
    )

    document.paragraphs[477].text = (
        "На основе анализа свойств изображений различных типов, а также характеристик форматов JPEG, PNG, WebP и AVIF "
        "в работе принято следующее распределение предпочтительных форматов сжатия. Такое распределение не является "
        "готовой табличной нормой из одного источника, а представляет собой проектное решение, сформированное по "
        "сравнительным характеристикам указанных кодеков."
    )

    document.paragraphs[478].text = (
        "Для фотографических изображений рассматриваются форматы AVIF, WebP и JPEG: AVIF и WebP ориентированы на "
        "эффективное сжатие полноцветных изображений при сохранении визуального качества, а JPEG включается как широко "
        "распространенный базовый формат сжатия фотографий с потерями. Для текстовой графики и снимков интерфейса "
        "приоритет отдается PNG как формату без потерь с поддержкой прозрачности; в качестве дополнительного варианта "
        "рассматривается WebP, для которого официальный инструмент cwebp предусматривает специальные режимы text и "
        "drawing. Для иллюстраций используются PNG, WebP и AVIF, поскольку для этого класса изображений важно сочетать "
        "поддержку прозрачности, возможность выбора между lossless- и lossy-представлением и компактность хранения. "
        "Для изображений смешанного типа в качестве универсальных вариантов рассматриваются WebP и JPEG."
    )

    document.paragraphs[479].text = (
        "Для каждого формата задаётся диапазон допустимых значений качества и шаг первичного перебора (формула 2.12). "
        "В качестве опорных уровней для lossy-кодеков используются типовые практические значения около 75 как базового "
        "уровня сжатия и диапазон 90-100 как уровня повышенного визуального качества. В документации WebP значение q=75 "
        "указано как стандартное значение по умолчанию, а q=100 соответствует наилучшему качеству; для PNG значение 100 "
        "в модели используется как условный маркер режима без потерь. Для AVIF диапазон качеств смещён немного ниже по "
        "сравнению с WebP, поскольку этот формат обеспечивает меньший размер файла при сопоставимом визуальном восприятии. "
        "В программной реализации первичный перебор ведётся по редкой сетке с шагом 5 единиц внутри выбранного диапазона, "
        "после чего выполняется локальное уточнение около лучшего найденного значения."
    )

    document.paragraphs[486].text = (
        "После этого для каждого результата вычисляется оценка визуального качества и оценка степени уменьшения размера. "
        "Визуальная составляющая определяется через среднеквадратическую ошибку, преобразованную в нормированную метрику "
        "качества, как формула 2.15"
    )

    document.paragraphs[491].text = (
        "где S(·) - функция размера файла. В программной реализации значение Z(x, y_f,q) дополнительно ограничивается "
        "интервалом [0; 1], чтобы исключить отрицательные значения и значения больше единицы."
    )

    document.paragraphs[496].text = (
        "где α определяет вклад визуального качества изображения, а β - вклад степени уменьшения размера файла. "
        "В рассматриваемой реализации принята нормированная пара коэффициентов α = 0,9 и β = 0,1. Такое соотношение "
        "соответствует приоритету визуального качества над дополнительным уменьшением объёма файла и может быть "
        "интерпретировано как результат многокритериального выбора по методу анализа иерархий при отношении важности "
        "критериев 9:1."
    )

    document.paragraphs[505].text = (
        "В итоге, алгоритм рекомендации включает два этапа: сначала выполняется грубый перебор кандидатов в пределах "
        "заданных диапазонов качества, затем проводится уточнение качества вблизи лучшего найденного значения. Это "
        "позволяет повысить точность выбора параметров без полного перебора всех значений качества от 0 до 100."
    )

    document.paragraphs[547].text = (
        "После выбора рекомендуемых параметров система выполняет преобразование исходного изображения в целевой формат. "
        "В подразделах 2.2.3-2.2.6 приводятся математические модели используемых форматов JPEG, WebP, PNG и AVIF. "
        "В программной реализации непосредственное кодирование выполняется готовыми энкодерами библиотеки Imagick и "
        "внешней утилитой avifenc, поэтому данные формулы описывают внутреннюю логику применяемых кодеков, а не ручную "
        "реализацию всех стадий кодирования в PHP-коде."
    )

    replace_math_text(document.paragraphs[472], "WebP,AVIF", "AVIF,WebP,JPEG")
    replace_math_text(document.paragraphs[475], "WebP,AVIF,PNG", "PNG,WebP,AVIF")
    replace_math_text(document.paragraphs[495], "α=0,92,    β=0,08", "α=0,9,    β=0,1")


def main() -> None:
    src = Path(r"C:\Users\aricr\Desktop\Microservices\source_vkr.docx")
    dst = Path(r"C:\Users\aricr\Desktop\Microservices\vkr_ranges_aligned.docx")

    document = Document(src)
    update_chapter_two(document)
    apply_citations(document)
    replace_source_list(document)
    normalize_document_font(document)
    document.save(dst)
    print(dst)


if __name__ == "__main__":
    main()
